const Layout: JSX.Component = () => {
  return (
    <component>
      <nav>
        <a href="/">Home</a>
        <a href="/about">About</a>
        <a href="/contacts">Contacts</a>
      </nav>

      <main data-nest></main>
    </component>
  )
}

export default Layout
